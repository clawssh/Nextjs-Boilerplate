// Middleware for authentication and other server-side logic
// This will be implemented when you add your authentication system

export function middleware(request: Request) {
  // Add your middleware logic here
  return;
}

export const config = {
  matcher: ['/((?!_next/static|_next/image|favicon.ico).*)'],
}; 